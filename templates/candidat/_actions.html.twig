<div class="d-print-none">
    <a href="{{ path('candidat_index') }}" class="btn btn-primary" role='button'><i class="ti ti-list"></i>
        Retour à la liste
    </a>

    <a href="{{ path('candidat_edit', { 'id': candidat.id }) }}" role="button"
       class="btn btn-primary"><i class="ti ti-pencil"></i> Editer</a>

    {% if is_granted('ROLE_MERITE_ADMIN') %}
        <button class="btn btn-danger" role='button' data-bs-toggle="modal" data-bs-target="#deleteModal"><i
                    class="ti ti-trash"></i>
            Supprimer
        </button>
    {% endif %}

    <br/>
    <br/>
</div>