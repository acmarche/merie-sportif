{{ texte|nl2br }}

<br/><br/>

<a href="{{ url('merite_token_show',{'value':value}) }}">
    {% if merite_vote_activate == 'true' %}
        Voter pour les candidats
    {% else %}
        Proposer des candidats
    {% endif %}
</a>
<br/><br/>
